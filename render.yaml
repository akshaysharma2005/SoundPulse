services:
  - type: web
    name: soundpulse
    runtime: python
    rootDir: .
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn dashboard.app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
